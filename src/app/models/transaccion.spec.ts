/*
import { Transaccion } from './transaccion';

describe('Transaccion', () => {
  it('should create an instance', () => {
    // Se comenta temporalmente porque el constructor de Transaccion requiere parámetros
    // expect(new Transaccion()).toBeTruthy();
  });
});
*/
